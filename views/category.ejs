<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="../css/loader.css" />
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="../css/base.css" />
    <link rel="stylesheet" href="../css/header.css" />
    <link rel="stylesheet" href="../css/home.css" />
    <link rel="stylesheet" href="../css/groups.css" />
    <link rel="stylesheet" href="../font-awsome-icons/css/all.css" />
    <link rel="icon" href="../font/others/new.png" type="image/x-icon" />
    <link
      rel="stylesheet"
      href="../material-icons/css/materialdesignicons.min.css"
      media="all"
    />

    <title>Document</title>
  </head>

  <body oncontextmenu="return false">
    <div id="header-container"><%- include('header') %></div>

    
    <div id="main-container">
    

<!-- <br> -->

      <div id="demo">
        <!-- <div id="weekly-deal">Weakly deal</div> -->
        <% Object.keys(ITEMS).forEach(key => { var row = ITEMS[key].toJSON();
        const percentOff = (row.off/100)*row.price; const oldPriceWhole =
        Math.round((row.price + percentOff) * (10**2)); const oldPriceRounded =
        oldPriceWhole / (10**2); const total = (row.rate) * 5; const rate =
        Math.round((row.stars / total) * 5); %>
        <div id="item">
          <form onsubmit="shew()" action="/det" method="post">
            <input type="hidden" name="id" value="<%=row.itemId%>" />
            <button id="image-btn">
              <img id="item-img" src="../font/<%=row.pic%>" alt="" srcset="" />
            </button>
          </form>
          <div id="item-name"><%=row.name%></div>
          <div id="prices">
            <div id="new-price">GH ₵ <%=row.price.toFixed(2)%></div>

            
          </div>

          <div id="percent-off">
           
            <% if (row.off <= 0) { %> <% }else{ %>
            <div id="old-price">GH ₵ <%=oldPriceRounded%></div>
            <% } %>

            <% if (row.off <= 0) { %> <% }else{ %>
            <span class="percent-off">-<%=row.off%>%</span>
            <% } %>
          </div>
        </div>

        <%})%> <% Object.keys(ITEMS).forEach(key => { var row =
        ITEMS[key].toJSON(); const percentOff = (row.off/100)*row.price; const
        oldPriceWhole = Math.round((row.price + percentOff) * (10**2)); const
        oldPriceRounded = oldPriceWhole / (10**2); const total = (row.rate) * 5;
        const rate = Math.round((row.stars / total) * 5); %>
        <div id="item">
          <form onsubmit="shew()" action="/det" method="post">
            <input type="hidden" name="id" value="<%=row.itemId%>" />
            <button id="image-btn">
              <img id="item-img" src="../font/<%=row.pic%>" alt="" srcset="" />
            </button>
          </form>
          <div id="item-name"><%=row.name%></div>
          <div id="prices">
            <div id="new-price">GH ₵ <%=row.price.toFixed(2)%></div>

            <% if (row.off <= 0) { %> <% }else{ %>
            <div id="old-price">GH ₵ <%=oldPriceRounded%></div>
            <% } %>
          </div>

          <div id="percent-off">
            <div id="star">
              <%for(let i = 0; i < rate; i++){ %>&starf;<%}%><%for(let i = 0; i
              < (5 - rate); i++){ %>&star;<%}%>(<%=row.rate%>)
            </div>

            <% if (row.off <= 0) { %> <% }else{ %>
            <span class="percent-off">-<%=row.off%>%</span>
            <% } %>
          </div>
        </div>

        <%})%> <% Object.keys(ITEMS).forEach(key => { var row =
        ITEMS[key].toJSON(); const percentOff = (row.off/100)*row.price; const
        oldPriceWhole = Math.round((row.price + percentOff) * (10**2)); const
        oldPriceRounded = oldPriceWhole / (10**2); const total = (row.rate) * 5;
        const rate = Math.round((row.stars / total) * 5); %>
        <div id="item">
          <form onsubmit="shew()" action="/det" method="post">
            <input type="hidden" name="id" value="<%=row.itemId%>" />
            <button id="image-btn">
              <img id="item-img" src="../font/<%=row.pic%>" alt="" srcset="" />
            </button>
          </form>
          <div id="item-name"><%=row.name%></div>
          <div id="prices">
            <div id="new-price">GH ₵ <%=row.price.toFixed(2)%></div>

            <% if (row.off <= 0) { %> <% }else{ %>
            <div id="old-price">GH ₵ <%=oldPriceRounded%></div>
            <% } %>
          </div>

          <div id="percent-off">
            <div id="star">
              <%for(let i = 0; i < rate; i++){ %>&starf;<%}%><%for(let i = 0; i
              < (5 - rate); i++){ %>&star;<%}%>(<%=row.rate%>)
            </div>

            <% if (row.off <= 0) { %> <% }else{ %>
            <span class="percent-off">-<%=row.off%>%</span>
            <% } %>
          </div>
        </div>

        <%})%>
      </div>

    </div>
    <%- include("footer")%>
  </body>
</html>

<script src="../js/slideshow.js"></script>
